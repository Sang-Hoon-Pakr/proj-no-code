# Server
PORT=3000
NODE_ENV=development

# Database (configure when DB module is added)
# DATABASE_URL=postgresql://user:pass@localhost:5432/messenger

# Redis (configure when WS/presence is added)
# REDIS_URL=redis://localhost:6379

# JWT (configure when auth is added)
# JWT_SECRET=
# JWT_ACCESS_TTL=15m
# JWT_REFRESH_TTL=14d
